import React from "react";
import Header from "./Header";

const Layout = () => {
    return (
        < > 
            <Header />
            <div>

            </div>
            <Footer />
        </>
    );
};

export default Layout;